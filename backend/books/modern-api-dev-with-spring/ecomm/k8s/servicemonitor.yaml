apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ecomm
  labels:
    app: ecomm
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: ecomm
  endpoints:
    - port: 8080-8080
      path: /actuator/prometheus
      interval: 15s
